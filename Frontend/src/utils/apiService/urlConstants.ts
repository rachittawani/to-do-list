const BASE_URL = '/api'

const USER = BASE_URL + '/users'

const UPDATEPASSWORD = BASE_URL + '/users/password'

const LOGIN = BASE_URL + '/auth/token'

const TODO = BASE_URL + '/todo'

const TAG = BASE_URL + '/link'

export {
    USER,
    UPDATEPASSWORD,
    LOGIN,
    TODO,
    TAG
}
